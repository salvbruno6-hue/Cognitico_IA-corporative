id: ELO-AUTO-001
title: Establish governed autonomous ELO execution loop
status: READY
risk: HIGH
owner: ELO
executor: Codex
repository: salvbruno6-hue/Cognitico_IA-corporative
base_branch: main

objective: >-
  Enable ELO to continue executable objectives through implementation,
  specialist review, architectural review, correction, validation, gated merge,
  post-merge verification, governed source/learning scans and terminal
  reporting without creating parallel cognitive authorities.

scope:
  include:
    - governed Chat ↔ Codex contract
    - agent execution workflow
    - task specification contract
    - internal Forge constructor plane and promotion boundary
    - merge and correction gates
    - cognitive execution supervision state and evidence
    - governed ELO scan orchestrator and its source/learning missions
  exclude:
    - new Cognitive Core
    - new memory authority
    - new Orchestrator or separate execution supervisor
    - provider-specific runtime implementation
    - destructive database changes
    - secret creation or rotation

specialists:
  required:
    - architecture
    - security
    - operations
    - testing
  optional:
    - data
    - domain

acceptance_criteria:
  - ELO Cognitivo owns task state, execution supervision, planning, specialist coordination, continuation, correction, replanning, escalation and promotion decisions.
  - ELO roles are explicit and non-overlapping.
  - Codex is defined as executor rather than sovereign decision-maker.
  - Specialist review and ELO review are separate gates.
  - Corrective cycles are bounded and repeatable.
  - Automatic merge requires explicit ELO approval and passing repository gates.
  - Direct writes to main are forbidden during task execution.
  - Forge is an internal constructor plane inside the canonical Cognitico repository, not a second Core, governance authority, shadow main, or independent cognitive system.
  - Historical/external Forge artifacts are evidence or candidates only; operational SQL, migrations, runtime configuration, dashboards and implementation artifacts are not promoted automatically.
  - Existing ELO-014+ governance is reused rather than duplicated.
  - Post-merge verification is part of the terminal state.
  - Every active task has a recoverable state, next action, evidence trail, cycle count and escalation condition.
  - The scan orchestrator is part of the existing Cognitivo execution loop and does not introduce a second supervisor or scheduler authority.
  - Daily and weekly scan missions are idempotent, scoped and provenance-preserving.
  - Scan discovery does not become learning without evidence, consistency, validation, governance and Evolution Gate.
  - Scan failures, stale sources and extraction gaps feed CONTINUE/CORRECT/REPLAN/ESCALATE decisions.
  - Inaccessible ChatGPT Project content is recorded as an ingestion gap rather than being treated as directly readable.

validation:
  - validate YAML task structure
  - inspect workflow syntax and permissions
  - inspect changed-file scope
  - confirm no duplicate Core, memory, Orchestrator or supervisor authority is introduced
  - confirm AGENTS and bridge contract agree on autonomous merge policy
  - confirm Cognitivo execution supervision is represented without a parallel supervisor component
  - confirm Forge is represented only as the internal constructor plane
  - validate ELO-SCAN-ORCHESTRATOR mission definitions and frequencies
  - validate scan-to-autocorrection transition rules
  - validate idempotence, provenance and source-scope constraints

autonomy:
  mode: agentic
  continue_until_terminal: true
  correction_cycles_max: 3
  allowed_terminal_states:
    - COMPLETED
    - BLOCKED
    - ESCALATED
    - FAILED
    - ROLLED_BACK

merge_policy:
  mode: automatic_when_gates_pass
  require_elo_decision: APPROVE_MERGE
  require_specialist_pass: true
  require_ci_pass: true
  require_acceptance_criteria: true
  require_no_blocking_findings: true
  require_scope_compliance: true
  allow_main_direct_push: false
  allow_bypass_branch_protection: false
  human_approval: conditional

evidence:
  required:
    - task_id
    - objective
    - current_state
    - next_action
    - current_cycle
    - specialist_findings
    - corrections
    - branch
    - commit_sha
    - changed_files
    - validation_results
    - pull_request
    - ci_results
    - elo_decision
    - merge_result
    - final_verification
    - learning
    - scan_results
    - ingestion_gaps
    - blockers

scan_integration:
  orchestrator: ELO-SCAN-ORCHESTRATOR
  daily_missions:
    - SCAN-001-source-inventory
    - SCAN-002-new-experiences
    - SCAN-003-errors-corrections
    - SCAN-004-arbitrated-decisions
    - SCAN-005-budget
  weekly_missions:
    - SCAN-006-repeated-patterns
    - SCAN-007-specializations
    - SCAN-008-evolution-gate
    - SCAN-009-unprocessed-sources
  correction_actions:
    stale_source: CORRECT
    extraction_gap: CORRECT
    repeated_failure: REPLAN
    authority_or_scope_conflict: ESCALATE
    insufficient_evidence: BLOCKED
    candidate_ready: EVOLUTION_GATE
  persistence_policy:
    reuse_existing_learning_structures: true
    historical_records_immutable: true
    canonical_mutation_by_scan: false
    automatic_learning_promotion: false
